pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "textile"
include(":androidApp")
include(":shared")
include(":compose-umbrella")
include(":desktop")